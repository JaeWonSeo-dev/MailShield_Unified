"""
레거시 Streamlit 데모 파일

중요:
- 이 프로젝트의 최종 제품은 웹페이지가 아니라 Chrome Extension입니다.
- 실제 사용자 경로는 `extension/` + `app/ml_api.py` 조합입니다.
- 이 파일은 과거 PshingMail_Detection 데모 흔적으로만 남겨둔 것이며,
  현재 통합 방향의 핵심 경로가 아닙니다.

권장 실행 경로:
1. `start-analysis.ps1`로 로컬 분석 API 실행
2. Chrome에서 `extension/` 로드
3. Gmail/Outlook 메일 열람 시 확장에서 자동 분석
"""

raise SystemExit(
    "streamlit_app.py is deprecated for this integrated project. "
    "Use the Chrome extension with app/ml_api.py instead."
)
